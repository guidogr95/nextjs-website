import { Input } from 'react-rainbow-components'

const index = (props) => {
    return (
        <Input
            {...props}
            className="themeInput"
        />
    )
}

export default index